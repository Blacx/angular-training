import { TurnBoldDirective } from './turn-bold.directive';

describe('TurnBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnBoldDirective();
    expect(directive).toBeTruthy();
  });
});
