import { TurnBoldNewDirective } from './turn-bold-new.directive';

describe('TurnBoldNewDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnBoldNewDirective();
    expect(directive).toBeTruthy();
  });
});
