import { TurnHighlightDirective } from './turn-highlight.directive';

describe('TurnHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
